sudo: required
os: linux
dist: trusty
group: edge

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y libdevmapper-dev

language: rust
rust: stable

branches:
    only: master

script: make check
